/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/energie-zdrazovani-model",
  trailingSlash: true,
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;
